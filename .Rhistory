set.seed(1)
rpois(5, 2)
set.seed(1)
rpois(5, 2)
?rnorm
?set.sedd
?set.seed
set.seed(1)
?rpois
?dpois
?qpois
set.seed(10)
x <- rep(0:1, each = 5)
e <- rnorm(10, 0, 20)
y <- 0.5 + 2 * x + e
y
library(datasets)
Rprof()
fit <- lm(y ~ x1 + x2)
Rprof(NULL)
library(datasets)
Rprof()
fit <- lm(y ~ x1 + x2)
Rprof(NULL)
system.time()
system.time()
system.time()
system.time()
system.time(
library(datasets)
Rprof()
fit <- lm(y ~ x1 + x2)
Rprof(NULL)
)
ss06 <- read.csv(download.file("https://d396qusza40orc.cloudfront.net/getdata%2Fdata%2Fss06hid.csv",
destfile="./SS06.csv",
method="curl")
)
download.file("https://d396qusza40orc.cloudfront.net/getdata%2Fdata%2Fss06hid.csv",
destfile="./SS06.csv",
method="curl")
ss06 <- read.csv("./SS06.csv")
names(ss06)
?tolower
tolower(names(ss06))
tolupper(names(ss06))
toupper(names(ss06))
tolower(names(ss06))
?strsplit
splitNames = strsplit(names(ss06),"\\.")
splitNames[[wgtp1:wgtp80]]
splitNames[[ss06$wgtp1:ss06$wgtp80]]
splitNames[[ss06$wgtp*]]
splitNames
splitNames = strsplit(names(ss06),"wgtp")
splitNames
download.file("https://d396qusza40orc.cloudfront.net/getdata%2Fdata%2FGDP.csv",
destfile="./GDP.csv",
method="curl")
gdp <- read.csv("./GDP.csv")
names(gdp)
head(gdp)
head(gdp$X.3)
head(gdp$X.3[5:])
tail(gdp$X.3)
gdp$X.3[5:207]
gdp$X.3[5:190]
numb <- gdp$X.3[5:190]
as.numeric(numb)
numb <- as.numeric(numb)
numb
numb <- gdp$X.3[5:190]
numb
sub(",", "", numb)
numb2 <- sub(",", "", numb)
numb
numb2
numb3 <- sub(",", "", numb2)
numb3
numb4 <- as.numeric(numb3)
numb4
mean(numb4)
head(gdp)
gdp$X.3
numb <- gdp$X.3[5:235]
numb2 <- sub(",", "", numb)
numb3 <- sub(",", "", numb2)
numb4 <- as.numeric(numb3)
mean(numb4)
numb2
numb3
numb4
?mean
mean(numb4, na.rm=T)
mean(numb4, na.rm=TRUE)
GDP <- read.csv(file.dest, skip=4, nrows=190)
GDPdol <- gsub(",", "", GDP$X.4)
GDP <- read.csv("./GDP.csv", skip=4, nrows=190)
GDPdol <- gsub(",", "", GDP$X.4)
GDPdol <- as.integer(GDPdol)
mean(GDPdol, na.rm=TRUE)
attach(GDP)
grep("^United",GDP$V4)
grep("*United",countryNames), 5
grep("^United",countryNames), 3
grep("^United",countryNames), 4
grep("United$",countryNames), 3
file.url <- 'https://d396qusza40orc.cloudfront.net/getdata%2Fdata%2FGDP.csv'
file.dest <- 'GDP4.csv'
download.file(file.url, file.dest, method='wget')
GDP <- read.csv(file.dest, skip=4, nrows=190)
file.url <- 'https://d396qusza40orc.cloudfront.net/getdata%2Fdata%2FEDSTATS_Country.csv'
file.dest <- 'edu.csv'
download.file(file.url, file.dest, method='wget')
edu <- read.csv(file.dest)
# merge the datasets
merged <- merge(GDP, edu, by.x = 'X', by.y = 'CountryCode')
# extract the information
fy.june <- grep('Fiscal year end: June', merged$Special.Notes)
length(fy.june)
file.url <- "https://d396qusza40orc.cloudfront.net/getdata%2Fdata%2FGDP.csv"
file.dest <- "GDP4.csv"
download.file(file.url, file.dest, method="curl")
GDP <- read.csv(file.dest, skip=4, nrows=190)
file.url <- "https://d396qusza40orc.cloudfront.net/getdata%2Fdata%2FEDSTATS_Country.csv"
file.url <- "https://d396qusza40orc.cloudfront.net/getdata%2Fdata%2FEDSTATS_Country.csv"
file.dest <- "edu.csv"
download.file(file.url, file.dest, method="curl")
edu <- read.csv(file.dest)
merged <- merge(GDP, edu, by.x = 'X', by.y = 'CountryCode')
fy.june <- grep('Fiscal year end: June', merged$Special.Notes)
length(fy.june)
library(quantmod)
install.packages("quantmod")
library(quantmod)
amzn = getSymbols("AMZN",auto.assign=FALSE)
sampleTimes = index(amzn)
year2012 <- grepl('2012-*', sampleTimes)
# count 2012 observations (i.e. true)
table(year2012)
# subset based on 2012
sampleTimes2012 <- subset(sampleTimes, year2012)
# convert to day of week
day <- format(sampleTimes2012, '%A')
# count each day
table(day)
sum(day)
day
library(swirl)
swirl()
x <- 1:4
p <- x/sum(x)
temp <- rbind(x, p)
rownames(temp) <- c("X", "Prob")
temp
mean(x)
mean(temp)
sum(x*p)
.75*.30
.48*.7
.336+.225
.225/.561
library(swirl)
swirl()
swirl()
1-(3/36)
deck
52
4/52
5/52
4/52
0
12/52
2/51
1/2*1/6
1.6*.8/2
.64
mypdf
?integrate
integrate(mypdf, lower=0, upper=1.6)
(x^2(/4)/2)
(x^2/4)/2
(2/4)/2
sqrt(2)
.997*.001
0.003*.999
.015*.999
(.997*.001)/(.997*.001+.015*.999)
library(swirl)
swirl()
6
(1+2+3+4+5+6)/6
expect_dice
dice_high
expect_dice(dice_high)
expect_dice(dice_low)
.5*(edh+edl)
integrate(myfunc, 0, 2)
spop
mean(spop)
allsam
apply(allsam, 1, mean)
mean(smeans)
library(RQDA)
RQDA()
library(knitr)
getwd()
setwd("./RepData_PeerAssessment1/")
unzip("activity.zip")
act <- read.csv(activity.csv)
act <- read.csv("activity.csv")
head(act)
tail(act)
act
names(act)
class(act)
class(act$steps)
class(act$date)
class(act$interval)
steps.date <- aggregate(steps ~ date, data=act, FUN=sum)
head(steps.date)
sd <- aggregate(steps ~ date, data=act, FUN=sum)
barplot(sd$steps, names.arg=steps.date$date, xlab="date", ylab="steps")
barplot(sd$steps, names.arg=steps.date$date, xlab="date", ylab="steps", color="red")
barplot(sd$steps, names.arg=steps.date$date, xlab="date", ylab="steps", col="red")
barplot(sd$steps, names.arg=steps.date$date, xlab="date", ylab="steps", col="blue")
sint <- aggregate(steps ~ interval, data=act, FUN=mean)
plot(sint, type="l")
plot(sint, type="l", xlab="Media actividad", ylab="Pasos")
plot(sint, type="l", xlab="Actividad media", ylab="Pasos")
hist(sd$steps, names.arg=sd$date, xlab="Fecha", ylab="Pasos", col="blue")
?hist
barplot(sd$steps, names.arg=sd$date, xlab="Fecha", ylab="Pasos", col="blue")
sint$interval[which.max(sint$steps)]
sum(is.na(act))
act2 <- merge(act, sint, by = "interval", suffixes = c("",
".y"))
head(act2)
act2
nas <- is.na(act2$steps)
act2$steps[nas] <- act2$steps.y[nas]
act2 <- act2[, c(1:3)]
head(act2)
act2
sda <- aggregate(steps ~ date, data = act2, FUN = sum)
barplot(sda$steps, names.arg = sda$date, xlab = "Fechas", ylab = "Pasos")
barplot(sda$steps, names.arg = sda$date, xlab = "Fechas", ylab = "Pasos", col="red")
day <- function(date) {
if (weekdays(as.Date(date)) %in% c("Saturday", "Sunday")) {
"weekend"
} else {
"weekday"
}
}
act$day <- as.factor(sapply(act$date, day))
par(mfrow = c(2, 1))
for (type in c("weekend", "weekday")) {
stype <- aggregate(steps ~ interval, data = act, subset = act$day ==
type, FUN = mean)
plot(stype, type = "l", main = type)
}
par(mfrow = c(2, 1))
for (type in c("weekend", "weekday")) {
stype <- aggregate(steps ~ interval, data = act, subset = act$day ==
type, FUN = mean)
plot(stype, type = "l", main = type)
}
par(mfrow = c(2, 1))
for (type in c("weekend", "weekday")) {
steps.type <- aggregate(steps ~ interval,
data = act,
subset = act$daytype == type,
FUN = mean)
}
activity
steps.date <- aggregate(steps ~ date, data=act, FUN=sum)
barplot(steps.date$steps, names.arg=steps.date$date, xlab="date", ylab="steps")
mean(steps.date$steps)
median(steps.date$steps)
steps.interval <- aggregate(steps ~ interval, data=act, FUN=mean)
plot(steps.interval, type="l")
steps.interval$interval[which.max(steps.interval$steps)]
act <- merge(act, steps.interval, by="interval", suffixes=c("",".y"))
nas <- is.na(act$steps)
act$steps[nas] <- act$steps.y[nas]
act <- act[,c(1:3)]
day <- function(date) {
if (weekdays(as.Date(date)) %in% c("Saturday", "Sunday")) {
"weekend"
} else {
"weekday"
}
}
act2$day <- as.factor(sapply(act2$date, day))
par(mfrow = c(2, 1))
for (type in c("weekend", "weekday")) {
steps.type <- aggregate(steps ~ interval,
data = act,
subset = act2$daytype == type,
FUN = mean)
plot(steps.type, type = "l", main = type)
}
```{r}
steps.date <- aggregate(steps ~ date, data=activity, FUN=sum)
barplot(steps.date$steps, names.arg=steps.date$date, xlab="date", ylab="steps")
```
2. Calculate and report the **mean** and **median** total number of
steps taken per day
```{r}
mean(steps.date$steps)
median(steps.date$steps)
```
## What is the average daily activity pattern?
1. Make a time series plot (i.e. `type = "l"`) of the 5-minute
interval (x-axis) and the average number of steps taken, averaged
across all days (y-axis)
```{r}
steps.interval <- aggregate(steps ~ interval, data=activity, FUN=mean)
plot(steps.interval, type="l")
```
2. Which 5-minute interval, on average across all the days in the
dataset, contains the maximum number of steps?
```{r}
steps.interval$interval[which.max(steps.interval$steps)]
```
## Imputing missing values
1. Calculate and report the total number of missing values in the
dataset (i.e. the total number of rows with `NA`s)
```{r}
sum(is.na(activity))
```
2. Devise a strategy for filling in all of the missing values in the
dataset. The strategy does not need to be sophisticated. For
example, you could use the mean/median for that day, or the mean
for that 5-minute interval, etc.
I will use the means for the 5-minute intervals as fillers for missing
values.
3. Create a new dataset that is equal to the original dataset but with
the missing data filled in.
```{r}
activity <- merge(activity, steps.interval, by="interval", suffixes=c("",".y"))
nas <- is.na(activity$steps)
activity$steps[nas] <- activity$steps.y[nas]
activity <- activity[,c(1:3)]
```
4. Make a histogram of the total number of steps taken each day and
Calculate and report the **mean** and **median** total number of
steps taken per day. Do these values differ from the estimates from
the first part of the assignment? What is the impact of imputing
missing data on the estimates of the total daily number of steps?
```{r}
steps.date <- aggregate(steps ~ date, data=activity, FUN=sum)
barplot(steps.date$steps, names.arg=steps.date$date, xlab="date", ylab="steps")
mean(steps.date$steps)
median(steps.date$steps)
```
The impact of the missing data seems rather low, at least when
estimating the total number of steps per day.
## Are there differences in activity patterns between weekdays and weekends?
1. Create a new factor variable in the dataset with two levels --
"weekday" and "weekend" indicating whether a given date is a
weekday or weekend day.
```{r, cache=TRUE}
daytype <- function(date) {
if (weekdays(as.Date(date)) %in% c("Saturday", "Sunday")) {
"weekend"
} else {
"weekday"
}
}
activity$daytype <- as.factor(sapply(activity$date, daytype))
```
2. Make a panel plot containing a time series plot (i.e. `type = "l"`)
of the 5-minute interval (x-axis) and the average number of steps
taken, averaged across all weekday days or weekend days
(y-axis).
```{r}
par(mfrow=c(2,1))
for (type in c("weekend", "weekday")) {
steps.type <- aggregate(steps ~ interval,
data=activity,
subset=activity$daytype==type,
FUN=mean)
plot(steps.type, type="l", main=type)
}
```
act <- read.csv("activity.csv")
sd <- aggregate(steps ~ date, data=act, FUN=sum)
barplot(sd$steps, names.arg=sd$date, xlab="Fecha", ylab="Pasos", col="blue")
sd <- aggregate(steps ~ date, data=act, FUN=sum)
barplot(sd$steps, names.arg=sd$date, xlab="Fecha", ylab="Pasos", col="blue")
mean(sd$steps)
median(sd$steps)
```
mean(sd$steps)
median(sd$steps)
sint <- aggregate(steps ~ interval, data=act, FUN=mean)
plot(sint, type="l", xlab="Actividad media", ylab="Pasos")
sint$interval[which.max(sint$steps)]
sum(is.na(act))
act2 <- merge(act, sint, by = "interval", suffixes = c("",
".y"))
nas <- is.na(act2$steps)
act2$steps[nas] <- act2$steps.y[nas]
act2 <- act2[, c(1:3)]
head(act)
head(act2)
sda <- aggregate(steps ~ date, data = act2, FUN = sum)
barplot(sda$steps, names.arg = sda$date, xlab = "Fechas", ylab = "Pasos", col="red")
mean(sda$steps)
median(sda$steps)
daytype <- function(date) {
if (weekdays(as.Date(date)) %in% c("Saturday", "Sunday")) {
"weekend"
} else {
"weekday"
}
}
act2$daytype <- as.factor(sapply(activity$date, daytype))
act2$daytype <- as.factor(sapply(act2$date, daytype))
head(act2)
tail(act2)
act2
par(mfrow = c(2, 1))
for (type in c("weekend", "weekday")) {
steps.type <- aggregate(steps ~ interval,
data = act2,
subset = act2$daytype == type,
FUN = mean)
plot(steps.type, type = "l", main = type)
}
activity <- read.csv("activity.csv")
activity <- merge(activity, steps.interval, by="interval", suffixes=c("",".y"))
nas <- is.na(activity$steps)
activity$steps[nas] <- activity$steps.y[nas]
activity <- activity[,c(1:3)]
steps.date <- aggregate(steps ~ date, data=activity, FUN=sum)
steps.interval <- aggregate(steps ~ interval, data=activity, FUN=mean)
activity <- merge(activity, steps.interval, by="interval", suffixes=c("",".y"))
nas <- is.na(activity$steps)
activity$steps[nas] <- activity$steps.y[nas]
activity <- activity[,c(1:3)]
steps.date <- aggregate(steps ~ date, data=activity, FUN=sum)
daytype <- function(date) {
if (weekdays(as.Date(date)) %in% c("Saturday", "Sunday")) {
"weekend"
} else {
"weekday"
}
}
activity$daytype <- as.factor(sapply(activity$date, daytype))
par(mfrow=c(2,1))
for (type in c("weekend", "weekday")) {
steps.type <- aggregate(steps ~ interval,
data=activity,
subset=activity$daytype==type,
FUN=mean)
plot(steps.type, type="l", main=type)
}
for (type in c("weekend", "weekday")) {
steps.type <- aggregate(steps ~ interval,
data=act2,
subset=act2$daytype==type)
plot(steps.type, type="l", main=type)
}
steps.type <- aggregate(steps ~ interval,
data=act2,
subset=act2$daytype==type
FUN=mean)
for (type in c("weekend", "weekday")) {
steps.type <- aggregate(steps ~ interval,
data=act2,
subset=act2$daytype==type
FUN=mean)
plot(steps.type, type="l", main=type)
}
par(mfrow=c(2,1))
for (type in c("weekend", "weekday")) {
steps.type <- aggregate(steps ~ interval,
data=act2,
subset=act2$daytype==type
FUN=mean)
plot(steps.type, type="l", main=type)
}
par(mfrow=c(2,1))
for (type in c("weekend", "weekday")) {
steps.type <- aggregate(steps ~ interval,
data=act2,
subset=act2$daytype==type,
FUN=mean)
plot(steps.type, type="l", main=type)
}
data <- read.csv("activity.csv")
library(ggplot2)
total.steps <- tapply(data$steps, data$date, FUN=sum, na.rm=TRUE)
qplot(total.steps, binwidth=1000, xlab="total number of steps taken each day")
mean(total.steps, na.rm=TRUE)
median(total.steps, na.rm=TRUE)
total.steps <- tapply(data$steps, data$date, FUN=sum, na.rm=TRUE)
qplot(total.steps, binwidth=1000, xlab="total number of steps taken each day")
qplot(total.steps, xlab="total number of steps taken each day")
averages <- aggregate(x=list(steps=data$steps), by=list(interval=data$interval),
FUN=mean, na.rm=TRUE)
ggplot(data=averages, aes(x=interval, y=steps)) +
geom_line() +
xlab("5-minute interval") +
ylab("average number of steps taken")
unzip("activity.zip")
#unzip("activity.zip")
library(ggplot2)
total.steps <- tapply(data$steps, data$date, FUN = sum, na.rm = TRUE)
qplot(total.steps, binwidth = 1000, xlab = "total number of steps taken each day")
